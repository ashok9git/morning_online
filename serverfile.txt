creating from git hub
Limit the commits output to ones with reflog entries
When --show-notes is in effect, the message from the notes
Show only commits in the specified revision range. 
When no <revision range> is specified, it defaults to 
HEAD (i.e. the whole history leading to the current commit).